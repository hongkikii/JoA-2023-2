spring:
  profiles:
    active: prod

  datasource:
    primary:
      hibernate:
        hbm2ddl:
#          auto: create
          auto: validate
    secondary:
      hibernate:
        hbm2ddl:
#          auto: create
          auto: validate
